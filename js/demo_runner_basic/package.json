{
  "name": "visualblocks-demo",
  "version": "0.0.1",
  "main": "script.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@visualblocks/runner": "file:../runner"
  },
  "devDependencies": {
    "esbuild": "0.19.3",
    "typescript": "5.2.2"
  },
  "scripts": {
    "build-deps": "cd ../runner && yarn build",
    "build": "../../scripts/build-js-demo.sh",
    "build-all": "yarn build-deps && yarn --force && yarn build"
  }
}
